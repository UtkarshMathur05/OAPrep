fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
# EmailStr needs this and pydantic does not pull it in; it was only present
# here transitively, so a fresh install would have failed on the auth schemas.
email-validator>=2.0
pydantic-settings>=2.3
psycopg[binary]>=3.1
pgvector>=0.2.5
httpx>=0.27
# Auth. argon2 is the current password-hashing recommendation; itsdangerous
# signs the OAuth state and the stateless email tokens.
argon2-cffi>=23.1
itsdangerous>=2.2
python-dotenv>=1.0
google-genai>=1.0.0
pytest>=8.0
