// Memory -> genome -> candidates -> reconstructed problem.
// TODO(frontend): wire services/api.ts through the components.
export default function Reconstruct() {
  return <main className="p-8">Reconstruct</main>
}
